-- Script to compute the score average of all records in the table second_table of the database hbtn_0c_0

-- Select the average score from the second_table
SELECT AVG(score) AS average FROM second_table;
