-- Script to list all records of the table second_table ordered by score in descending order

-- Select the score and name columns from the second_table and order by score in descending order
SELECT score, name FROM second_table ORDER BY score DESC;
